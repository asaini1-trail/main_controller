% Load once
% waypoints = readmatrix('output.csv');

% In MATLAB command window or script
waypoints = readmatrix('output.csv');

% Or already converted local coords
% waypoints = readmatrix('rectangle_path.csv');
% waypoints_x = waypoints(:,1);
% waypoints_y = waypoints(:,2);